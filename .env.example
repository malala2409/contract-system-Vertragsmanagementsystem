# Copy this file to .env and fill in your database config
# If no .env exists, the app defaults to SQLite (contract.db)

# MySQL example:
# DATABASE_URL=mysql+pymysql://user:password@host:port/database_name

# PostgreSQL example:
# DATABASE_URL=postgresql://user:password@host:port/database_name
